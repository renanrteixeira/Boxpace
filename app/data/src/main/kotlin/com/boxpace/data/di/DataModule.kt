package com.boxpace.data.di

/**
 * Ponto de injeção de dependência da camada `data` — **esqueleto**.
 * A fiação completa (Koin/Hilt) é consolidada em stories posteriores.
 */
object DataModule {
    // TODO: fiação das implementações (remote/local/cloud) para o app.
}
