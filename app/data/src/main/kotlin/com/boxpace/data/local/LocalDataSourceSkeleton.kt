package com.boxpace.data.local

/**
 * Implementação local (Room) da persistência de rastreio — **esqueleto**.
 * A persistência efetiva (entidades, DAOs, espelho da fonte canônica) é a Story 1.6.
 */
object LocalDataSourceSkeleton {
    // TODO(1.6): Room Database, DAOs e espelho local.
}
