package com.boxpace.data.cloud

/**
 * Implementação cloud (Google Drive) da persistência — **esqueleto**.
 * O plano OAuth/Drive e o JSON round-trip são o Epic 5.
 */
object CloudDataSourceSkeleton {
    // TODO(5.x): OAuth Google e sincronização via arquivo JSON no Drive.
}
